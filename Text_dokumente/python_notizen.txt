(FROM eth-network folder)
# create and activate a venv
python3 -m venv .venv
source .venv/bin/activate

# install deps inside the venv
python3 -m pip install --upgrade pip
python3 -m pip install web3 requests

# run your script
python3 ./python/baseline_tx.py